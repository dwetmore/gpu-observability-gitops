# gpu-observability-gitops
Pure GitOps (Path A) GPU observability stack for single-node MicroK8s on a laptop (RTX 5000). Everything MUST be deployed from this repo via ArgoCD (no microk8s enable observability, no microk8s enable gpu).
